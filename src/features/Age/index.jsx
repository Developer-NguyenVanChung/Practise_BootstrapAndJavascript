import React from 'react';
import PropTypes from 'prop-types';
import { Container, Row, Col, Button } from 'react-bootstrap'


Age.propTypes = {

};

function Age({ data }) {
    // var ageList = [];
    // data.map((item) => {
    //     let userAge = new Date().getFullYear() - item.dateOfBirth
    //     if (!ageList.includes(userAge)) {
    //         ageList.push(userAge)
    //     }
    //     var ageListSort = ageList.sort((a, b) => a - b);

    //     console.log(ageListSort);
    // }
        return (
            <Container>
                13
            </Container >
        );
    


}
export default Age;